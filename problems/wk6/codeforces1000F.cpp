#include <iostream>
#include <cstdio>
#include <algorithm>
using namespace std;
//one ocurrence

const int maxn = 5e5 + 10;
int N , Q;
int a[maxn];



int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cin >> N;
  for(int i = 1; i <= N; ++i)cin >> a[i];
  cin >> Q;
  for(int i = 1; i <= Q; ++i)
}
